<!-- experimental_results / complexity 1 / 2MD -->

# Complexity 1 — 2MD

## Overview

**2MD** is a comparative experiment at complexity level 1, designed to be run alongside or after 1UP. The exact definition (e.g. Meta evolution using Darwinian meta algorithm) is set by the experiment plan.

## Purpose

- Compare Lamarckian and Darwinian (or a variant) under the same complexity-1 conditions.
- Provide an overview and explanation of the 2MD setup, levers, and where results are stored.

## Experiment design

*(To be filled when 2MD is defined: topology, initial conditions, call budget, and which levers are fixed vs tuned.)*

## Where results go

- **Generated results:** place run outputs (e.g. `Test_2MD.md` or similar) in a `generated results/` subfolder under this directory.

## How to run

*(Add the command or script used to run 2MD once it exists.)*

## Complexity level

**Complexity 1** — same base problem class as 1UP (e.g. Rastrigin, 300 calls). 2MD at complexity 2 would use the same experiment design at a higher complexity level.
