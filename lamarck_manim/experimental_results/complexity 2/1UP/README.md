<!-- experimental_results / complexity 2 / 1UP -->

# Complexity 2 — 1UP: Unoptimized Pure Lamarckian v Darwinian

## Overview

**1UP at complexity 2** is the same comparative experiment as 1UP (Unoptimized Pure Lamarckian v Darwinian) but at a higher complexity level—e.g. different topology, larger call budget, or harder landscape—to test whether the baseline comparison holds as problem difficulty increases.

## Purpose

- Re-run the 1UP design (same initial-condition and random-lever setup) under complexity-2 conditions.
- Compare Lamarckian vs Darwinian when the problem is harder or the budget differs from complexity 1.

## Experiment design

| Aspect | Complexity 1 (reference) | Complexity 2 |
|--------|---------------------------|--------------|
| **Design** | Same as 1UP: 4 random points, random levers, same budget for both. | Same design; complexity-2 topology/budget/dimension (TBD). |
| **Topology** | Rastrigin. | *(e.g. different scale, dimension, or landscape—to be set.)* |
| **Termination** | 300 calls. | *(e.g. 500 or 1000 calls—to be set.)* |

*(Update the table when complexity 2 parameters are fixed.)*

## Where results go

- **Generated results:** place run outputs in a `generated results/` subfolder under this directory (e.g. `Test_1_UP1_complexity2.md`).

## How to run

*(Add the command or script for running 1UP at complexity 2 once implemented.)*

## Complexity level

**Complexity 2** — harder or larger instance than complexity 1. Exact definition (topology, bounds, call budget) to be documented here when set.
